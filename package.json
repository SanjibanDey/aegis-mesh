{
  "name": "aegis-mesh",
  "version": "1.0.0",
  "description": "Offline-first, decentralized state synchronization engine for disaster resource allocation over an ad-hoc BLE mesh (CRDT-based, digest-driven anti-entropy sync).",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "run:build": "node dist/index.js"
  },
  "engines": {
    "node": ">=18.17"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
